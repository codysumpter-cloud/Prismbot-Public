# Commit Style

Use concise, action-first messages:

- `Fix missing build:all script in CI path`
- `Add preflight doctor check before packaging`
- `Guard packager against stale release bundle`

Avoid vague messages like "updates" or "fix stuff".
