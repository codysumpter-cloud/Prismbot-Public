# Safe Baseline

Keep these set:

- `gateway.bind = loopback`
- auth rate limiting enabled
- Discord `groupPolicy = allowlist`
- Telegram `allowFrom` includes owner only

Do not loosen these without explicit user instruction.
