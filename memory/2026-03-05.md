
## 2026-03-05 12:10 UTC — New-thread one-pass execution directive from Prismtek

On next fresh thread, execute in one pass with these priorities:
1) Real image quality without OOM:
   - lightweight diffusers profile first (tiny model + capped resolution + queued runs)
   - memory guardrails before each run
   - guarded switch + memory caps so real model mode cannot knock service offline
2) Studio UX final pass:
   - simplify mode flow
   - clearer run timeline + retries
   - reduce end-user debug noise
3) Omni docs dev-ready:
   - copy-paste endpoint examples
   - status badges (real/partial/planned)
   - minimal JS/Python SDK snippets
4) Reliability hardening:
   - persistent queues for studio jobs (not only omni runs)
   - auto-retry policy + clearer user-facing errors
   - health dashboard card for local-image engine

Expectation: complete as much as possible in one coordinated pass before requesting follow-up.

## 2026-03-05 19:34 UTC — Omni text backend upgraded from stub to real local model

- Patched `apps/prismbot-core/src/omni/execution.js` so `provider=local` defaults to `OMNI_LOCAL_MODE=ollama` and calls Ollama `/api/chat` using full `messages[]` context.
- Added explicit mode control + guardrails:
  - `OMNI_LOCAL_MODE` (`ollama` default, `stub` optional)
  - `OMNI_LOCAL_MODEL` (set to `llama3.2:3b`)
  - `OMNI_LOCAL_OLLAMA_HOST`
  - `OMNI_LOCAL_ALLOW_STUB_FALLBACK` (set false)
- Installed Ollama runtime on host, pulled `llama3.2:3b`, enabled service.
- Updated `~/.config/prismbot-core.env` to enable local real-model path (`OMNI_OLLAMA_ENABLED=true`, local ollama vars) and restarted `prismbot-core.service`.
- Verified live call to `/api/omni/chat/completions` returns real model output (`model: llama3.2:3b`, backend active).

## 2026-03-05 19:52 UTC — Omni-owned model profile live

- Created Ollama model profile `omni-core:latest` from `llama3.2:3b` with Omni system behavior constraints (no echo default, execution-focused style).
- Added source profile file: `apps/prismbot-core/models/omni-core/Modelfile`.
- Set production runtime to use `OMNI_LOCAL_MODEL=omni-core:latest`.
- Verified `/api/omni/chat/completions` returns `model: omni-core:latest` with backend `local-ollama`.
- Tuned runtime stability knobs for heavy cold starts:
  - `OMNI_PROVIDER_TIMEOUT_MS=60000`
  - `OMNI_PROVIDER_MAX_ATTEMPTS=1`

## 2026-03-05 (UTC) — Durable updates (late session)

- Major platform push completed across PrismBot/OmniAPI with many commits pushed to `origin/main`; user is driving a "make Omni complete" mission and wants rapid execution over planning.
- User explicitly wants Omni sovereignty direction: disable dependency on external providers and build local methods that can replace provider capabilities; maintain OpenClaw/ChatGPT control-channel continuity while doing so.
- Applied live runtime config for local-only Omni mode in `~/.config/prismbot-core.env`:
  - `OMNI_ROUTING_STRATEGY=local-first`
  - external providers disabled (`OMNI_OPENAI_ENABLED=false`, `OMNI_ANTHROPIC_ENABLED=false`, `OMNI_GOOGLE_ENABLED=false`, `OMNI_XAI_ENABLED=false`, `OMNI_OLLAMA_ENABLED=false`)
  - `OMNI_LOCAL_ENABLED=true`
  - restarted `prismbot-core.service` successfully.
- User validated direct Omni chat via curl against `http://127.0.0.1:8799/api/omni/chat/completions` and received successful JSON response (`ok:true`).
- Production UX issue identified: end users should not receive local filesystem paths; implemented public asset URL shaping support and docs; internal paths remain private.
- User had a serious reliability scare (SSH unreachable); VM required full stop/start. Post-incident hardening was applied with user constraint: **do not risk lockout**.
  - Added safe fail2ban+sshd hardening and swap/memory tuning; reduced root disk pressure significantly.
  - User preference reinforced: avoid any lockout-risk changes unless explicitly approved.
- Telegram integration request: user wants Omni/OpenClaw reachable via Telegram; pending required inputs (BotFather token + Telegram numeric user ID) before config patch/restart.
- Quality bar note: user rejected current Omni pixel output quality as "trash" compared to Gemini sample; expects Omni to surpass that quality for pixel ZoomQuilt-style outputs.

## 2026-03-05 21:26 UTC — Canonical Omni mission clarified by Prismtek

- Omni is not just routing/API glue; it is intended to be an AI model + API platform.
- Primary target: replace cost-based AI models/APIs in practice (ChatGPT, Ollama, Grok, Nano Banana, PixelLab.ai equivalent coverage) with a local/open-source standalone Omni stack.
- Product policy (current phase): users should incur no usage cost until Prismtek explicitly chooses to introduce pricing.
- Scope baseline: Omni must at minimum fulfill service parity for capabilities those products provide (chat/reasoning + media/tooling surfaces), then exceed quality where possible.

